// Importar módulos
import { initFormHandlers, initFormSwitcher } from './forms.js';

// Inicializar todos los componentes
document.addEventListener('DOMContentLoaded', () => {
    initFormHandlers();
    initFormSwitcher();
});